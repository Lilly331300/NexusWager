import WaitlistPageClient from "../../components/waitlist/WaitlistPageClient";

export const metadata = {
  title: "Join the Waitlist | NexusWager",
  description:
    "Join the NexusWager waitlist for early updates on the competitive gaming ecosystem, game worlds, wallet layer, partners, and launch progress.",
};

export default function WaitlistPage() {
  return <WaitlistPageClient />;
}